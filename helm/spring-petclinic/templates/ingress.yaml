{{- if and .Values.petclinic.enabled .Values.petclinic.ingress.enabled -}}
{{- $fullName := include "spring-petclinic.app.fullname" . -}}
{{- $svcPort := .Values.petclinic.service.port -}}
{{- if and .Values.petclinic.ingress.className (not (hasKey .Values.petclinic.ingress.annotations "kubernetes.io/ingress.class")) }}
  {{- $_ := set .Values.petclinic.ingress.annotations "kubernetes.io/ingress.class" .Values.petclinic.ingress.className}}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "spring-petclinic.app.labels" . | nindent 4 }}
  {{- with .Values.petclinic.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.petclinic.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.petclinic.ingress.className }}
  {{- end }}
  {{- if .Values.petclinic.ingress.tls }}
  tls:
    {{- range .Values.petclinic.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.petclinic.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }} 